import { StepProps } from "../types/types";

function Step10Results({
  currentStep,
  setCurrentStep,
  userResponses,
  setUserResponses,
  questionPrompts,
  setQuestionPrompts,
}: StepProps) {
  return <>test</>;
}

export default Step10Results;
